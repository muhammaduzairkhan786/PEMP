{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "UseSqlite": true,
  "ConnectionStrings": {
    "Pemp": "Data Source=pemp-demo.db"
  },

  "//AzureAd": "Leave ClientId blank for the local demo (role-switcher fallback). Set all three (+ UseSqlite:false + a SqlServer ConnectionStrings:Pemp) to enable Entra SSO — see docs/azure-entra-setup.md.",
  "AzureAd": {
    "Instance": "https://login.microsoftonline.com/",
    "TenantId": "",
    "ClientId": "",
    "CallbackPath": "/signin-oidc"
  }
}
